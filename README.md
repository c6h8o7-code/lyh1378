## lyh1378全物品假人使用指南
### 下载配置
1. 下载nodejs并配置环境变量(如果已有，跳过这一步)
2. 执行 `npm install mineflayer` 与 `npm install mineflayer-pathfinder`
3. 下载git并配置环境变量(如果已有，跳过这一步)
4. 在你想放程序的文件夹里shift+右键打开终端，执行 `git clone xxx`
5. 执行 `nodejs bot.ts`即可
### 功能
1. 各类命令，可在游戏内输入`1378 --help`
2. 在终端可控制其说话
3. 在终端获取服务器消息
